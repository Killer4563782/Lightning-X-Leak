#include "commands/types/bool_command.hpp"

namespace commands
{
	bool_command g_force_script_host("Force Script Host", "force_script_host", "Force script host for a faster session joining and taking the script host");
}